<div id="${id}" class="multi-checkbox">
    <label for="${id}">
        ${label.encodeAsHTML()}
    </label>
    <g:checkBox  name="${name}" id="${id}" value="${value}" checked="${checked}" class="${classes}" />
</div>